{
  "name": "paperclip-runs",
  "category": "app",
  "description": "Queue-backed bridge from Paperclip's http adapter to Hermes/Openclaw with async re-wake callbacks (overlay-only)",
  "depends_on": ["redis"],
  "env": [
    { "name": "BRIDGE_TOKEN",      "generate": "openssl rand -hex 32", "hide": true },
    { "name": "PROVIDER",          "default": "hermes",                "prompt": "Provider that serves the runs (hermes | openclaw)" },
    { "name": "PROVIDER_BASE_URL", "default": "http://hermes:8642",    "prompt": "Provider OpenAI-compatible base URL" },
    { "name": "PROVIDER_API_KEY",  "from_state": "HERMES_API_KEY",     "hide": true },
    { "name": "PROVIDER_MODEL",    "default": "hermes-agent",          "prompt": "Model id (hermes-agent | openclaw/default)" },
    { "name": "PAPERCLIP_API_URL", "default": "http://paperclip:3100", "prompt": "Paperclip overlay base URL for the wakeup callback" }
  ]
}
