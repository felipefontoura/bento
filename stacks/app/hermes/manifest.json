{
  "name": "hermes",
  "category": "app",
  "description": "OpenAI-compatible AI agent gateway (NousResearch Hermes, overlay-only)",
  "depends_on": [],
  "env": [
    { "name": "HERMES_API_KEY",     "generate": "openssl rand -hex 32", "hide": true },
    { "name": "OPENROUTER_API_KEY", "prompt": "OpenRouter API key (optional — at least one provider required for Hermes to serve a model)", "hide": true }
  ]
}
