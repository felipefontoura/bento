{
  "name": "hermes",
  "category": "app",
  "description": "Seeds a shared named volume with the upstream Hermes Agent /opt/hermes tree so peer containers (paperclip) can mount it and call the CLI via Paperclip's hermes_local adapter (overlay-only, idle sleep, no gateway)",
  "depends_on": [],
  "env": []
}
